type prime extends int:

	constraint:
		prime > 1
		prime % 2 != 0 or prime = 2
	for i in 3 : prime ^ (1/2) + 1 : 2:
		constraint:
			prime % i != 0

//for i in 0:20:1:
//	assert prime i:
//		print(i)

link test